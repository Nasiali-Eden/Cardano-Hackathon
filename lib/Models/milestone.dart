class Milestone {
  final String id;
  final String title;
  final String description;
  final bool reached;

  const Milestone({
    required this.id,
    required this.title,
    required this.description,
    required this.reached,
  });
}
